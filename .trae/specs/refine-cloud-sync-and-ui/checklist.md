- [x] 编译脚本已包含 `--dart-define=ENCRYPTION_KEY=${{ secrets.KEY_PASSWORD }}`。
- [x] `CloudSyncService` 使用了环境变量中的密钥进行加密，且云端文件命名采用了中文。
- [x] 触发连续报错时，新报错会立即覆盖旧报错，不再出现排队等待现象。
- [x] 添加账户时，明确询问并记录了该账户是管理员还是普通用户。
- [x] 左上角状态指示灯能正确反映 WebDAV 连接状态，并在点击后弹出包含上传、下载、编辑的面板。
- [x] 普通用户的数据隔离存放于 `/用户名/数据/` 目录中，公共数据存放于 `/公共数据/` 中。
- [x] 管理员可通过弹窗进入文件管理器，具备查看、修改、删除、移动、复制所有用户云端文件的能力。
- [x] 应用内没有未实现的空壳按钮，所有主要功能（听写、同步、管理）均可正常使用。
